package compiler.dag;

public class DumperDAG {
  private final DAGNode root;

  public DumperDAG(DAGNode node) {
    assert node != null;
    this.root = node;
  }

  // ... I'm tired, can't do this now lol
}
